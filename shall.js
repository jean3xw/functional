//-- shall.js stand for secure holder active launcher loader // that seem meaningless but it's a cool name isn't it???
//-- ES 5 support?
$FJS(

var SecureHolderActiveLaunchedLoader = ( function ($FJS){
    return { 
        "$FJS" : $FJS 
    };
  
})( jQuery.sub() );